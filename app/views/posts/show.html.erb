<div class="container">
  
  <div class="text-center">
        <div class="card">
          <div class="card-body">
            <div class="card-title">
                <h3><%= @post.title %></h3>
            </div>
            <div class="row">
              <p class="mb-0">ユーザー名: <%=  link_to @post.user.name, @post.user %></p>
            　<p>投稿日:<%=@post.user.created_at.strftime('%Y/%m/%d') %></p>
            </div> 
            <div class="list-group list-group-flush">
            <div class="video-container">
              <iframe class="video-content" src="https://www.youtube.com/embed/<%= @video_id %>" frameborder="0" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" allowfullscreen></iframe>
            </div>
            <li class="list-group-item">
              <span style="text-decoration: underline"><h3>内容</h3></span>
              <%= simple_format(@post.content) %>
            </li>
            <li class="list-group-item"><%= @post.note %></li>
            </div>
          </div>
        <div class="pl-2">
              <% if logged_in?%>
                <% if current_user.fav?(@post) %>
                  <%= form_with(model: current_user.favorites.find_by(favorite_id: @post.id), local: true, method: :delete) do |f| %>
                  <%= hidden_field_tag :favorite_id, @post.id %>
                  <%= f.submit 'いいね解除', class: 'btn btn-danger btn-sm' %>
                <% end %>
                <% else %>
                  <%= form_with(model: current_user.favorites.build, local: true) do |f| %>
                  <%= hidden_field_tag :favorite_id, @post.id %>
                  <%= f.submit 'いいね', class: 'btn btn-primary btn-sm' %>
                  <% end %>
              <% end %>
            <% end %>
              </div>
  </div>